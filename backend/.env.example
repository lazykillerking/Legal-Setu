# Comma-separated list of origins allowed to call the API in dev/prod.
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Supabase (see /supabase/README.md) — not wired yet, placeholders only.
SUPABASE_URL=
SUPABASE_ANON_KEY=
